{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "agents",
  "description": "Multi-agent decomposition: planner/generator/reviewer pipeline triad with tool-isolated capability boundaries, plus a read-only researcher for pre-planning investigation. Includes /worktree-team for physical worktree isolation, directory-ownership enforcement (opt-in), and output-schema checks on generator exit.",
  "version": "1.3.0",
  "keywords": [
    "agents",
    "decomposition",
    "parallel",
    "pipeline"
  ]
}
