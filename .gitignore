# codegraph structural index (local-only, rebuilt per repo)
.codegraph/

# Per-session artifacts written by long-session/workflow plugins
.claude/
claude-progress.txt

# Local research material — not part of the marketplace
*.pdf

# JetBrains IDE config — local-only
.idea/
